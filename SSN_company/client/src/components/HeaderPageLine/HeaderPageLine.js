import React from "react";
import "./HeaderPageLine.scss";

const HeaderPageLine = ({user, index}) => {
    return (
        <div className="header_line"></div>
    );
};

export default HeaderPageLine;
